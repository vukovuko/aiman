{
  "engines": {
    "node": ">=20.6.0"
  },
  "name": "aiman-backend",
  "version": "1.0.0",
  "description": "AIMan backend implementation",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --env-file=.env --watch -r ts-node/register src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vuko Vukasinovic",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
